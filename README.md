# OpenSky.Net

OpenSky.NET is a library for interacting with the [OpenSky Network](https://opensky-network.org) API. The library is
under active development, so expect frequent changes over the next month.